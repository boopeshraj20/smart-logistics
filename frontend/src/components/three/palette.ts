/**
 * Shared material palette for the procedural 3D vehicle models.
 * Tuned for a premium enterprise look on a dark scene.
 */
export const palette = {
  /** Steel-blue primary body. */
  body: '#3d4a63',
  /** Lighter body variant (trailers / cargo boxes). */
  bodyAlt: '#495a78',
  /** Deeper accent for hoods and bumpers. */
  bodyDeep: '#2c3750',
  /** Brand accent, used for trims and details. */
  accent: '#4f8cff',
  /** Chassis, bumpers, structural parts. */
  chassis: '#171b25',
  /** Tyre rubber. */
  rubber: '#12141a',
  /** Rim metal. */
  rim: '#8f9ab0',
  /** Glass (cab windows). */
  glass: '#050a14',
  /** Headlight lens. */
  lens: '#dfe8ff',
} as const;